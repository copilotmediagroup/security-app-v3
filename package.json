{
  "name": "co-pilot-security-persistent-gps-status",
  "version": "3.0.18",
  "private": true,
  "description": "Adds timestamped Route/GPS map behavior and persists Guard Online status across logout/login until Offline is clicked.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}