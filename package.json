{
  "name": "co-pilot-security-server-version-qa-fix",
  "version": "3.0.64",
  "private": true,
  "description": "QA pass for v3.0.63 Report Archive Command Center. Fixes stale server startup version label while preserving Report Archive, Report Builder guard-name fix, and all no-SQL workflow requirements.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}
