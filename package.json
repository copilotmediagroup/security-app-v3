{
  "name": "co-pilot-security-client-properties-command-center",
  "version": "3.0.26",
  "private": true,
  "description": "Rebuilds the client Properties page into a premium command-center layout with searchable property list, detail rail, map, activity, and patrol visibility.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}