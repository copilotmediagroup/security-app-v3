{
  "name": "co-pilot-security-locked-workflow-stages",
  "version": "3.0.14",
  "private": true,
  "description": "Locks prior Guard Active Job workflow stages after the guard advances so completed stages cannot be reopened.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}
