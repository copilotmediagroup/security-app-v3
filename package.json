{
  "name": "co-pilot-security-client-map-live-sync",
  "version": "3.0.25",
  "private": true,
  "description": "Updates the client dashboard map to mirror the guard map behavior with client-only property visibility and live accepted-guard route tracking.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}