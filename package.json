{
  "name": "co-pilot-security-request-patrol-command-center",
  "version": "3.0.32",
  "private": true,
  "description": "Builds the Client Request Patrol page into a premium command-center workflow with request type selector, priority segment, services cards, device-only upload, summary rail, and history table.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}