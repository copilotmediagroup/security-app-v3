{
  "name": "co-pilot-security-dead-code-cleanup-button-qa",
  "version": "3.0.66",
  "private": true,
  "description": "Cleans duplicate handlers and stale UI paths, adds workflow state normalization, disables visual-only placeholder buttons, and adds submit double-click protection while preserving the global action locks.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}
