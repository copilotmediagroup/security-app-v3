{
  "name": "co-pilot-security-dispatch-live-gps-command-center",
  "version": "3.0.44",
  "private": true,
  "description": "Redesigns Dispatch Live GPS into a full command-center screen with KPI row, live map, online guard roster, selected property panel, GPS feed, route events, default map reset, layers, and fullscreen action.",
  "scripts": {
    "dev": "node server.cjs",
    "start": "node server.cjs",
    "preview": "node server.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {}
}