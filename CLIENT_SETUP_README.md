# Co Pilot Security v3.0.0 — Exact Dashboard Grid Rebuild

This package is built for your fresh Bolt / cleared GitHub workflow.

## What this is

A clean rebuild package that codes the dashboard layout exactly from the layout breakdown:

- 220px left sidebar
- main dashboard content area
- top header
- 4 equal KPI cards
- main grid split into wide left canvas + 340px right rail
- Dispatch Overview and Priority Queue top row
- large Dispatch Command Map
- Recent Activity table
- right rail with Active Patrols, Messages, Notifications, and System Status
- hard bottom-right build badge

## Badge

The bottom-right badge should show:

`v3.0.0 EXACT DASHBOARD GRID REBUILD`

## Bolt

This package includes:

- `npm run dev`
- `npm run start`
- `npm run preview`
- `npm run build`

Bolt should be able to run the preview automatically.

## Supabase

This package keeps the same Supabase config in `config.js`.

## SQL

No new SQL required.
