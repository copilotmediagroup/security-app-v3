# Co Pilot Security v3.0.62 — Report Builder Guard Name Fix

## What changed
- Fixed the `guardDisplayName is not defined` runtime error when opening Dispatch/Admin → Report Builder.
- Added the missing global `guardDisplayName(guard)` helper used by the Report Builder page.
- Preserved all v3.0.61 Report Builder Command Center features.

## SQL
No new SQL required.
